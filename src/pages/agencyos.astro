---
import LandingLayout from '../layouts/LandingLayout.astro';

const title = 'AgencyOS - Run Your Entire Agency with AI';
const description = '125+ AI commands for marketing, sales, affiliate, and finance. The one-person agency operating system.';
---

<LandingLayout title={title} description={description}>

<!-- Hero Section -->
<div class="relative overflow-hidden pb-10 lg:pb-16">
  <!-- Animated Background -->
  <div class="hero-bg-effects">
    <div class="gradient-orb orb-1"></div>
    <div class="gradient-orb orb-2"></div>
    <div class="gradient-orb orb-3"></div>
    <div class="grid-pattern"></div>
  </div>

  <div class="relative mx-auto max-w-content px-4 sm:px-6 lg:px-8 text-center">
    <div class="mx-auto max-w-3xl">
      <h1 class="text-4xl font-bold tracking-tight sm:text-6xl mb-6">
        <span class="static-text">Run your </span>
        <span class="typewriter-container">
          <span class="typewriter-text" id="typewriter"></span>
          <span class="cursor">|</span>
        </span>
        <span class="static-text"> with AI.</span>
      </h1>

      <p class="mt-4 text-xl leading-relaxed text-[var(--color-text-secondary)] mb-6">
        125+ commands. Zero employees. Infinite scale.
      </p>

      <p class="text-sm text-[var(--color-text-muted)] mb-10">
        Join 500+ agencies worldwide • 4.9/5 rating • 30-day guarantee
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-10">
        <a href="/docs/getting-started/installation" class="magnetic-btn inline-flex items-center justify-center px-8 py-3.5 text-base font-medium text-white bg-[var(--color-accent-green)] rounded-[var(--radius-md)] hover:brightness-110 transition-all duration-200 shadow-green hover:shadow-lg">
          Start Free Trial
        </a>
        <a href="#how-it-works" class="magnetic-btn inline-flex items-center justify-center px-8 py-3.5 text-base font-medium text-[var(--color-text-primary)] bg-[var(--color-bg-tertiary)] border border-[var(--color-border)] rounded-[var(--radius-md)] hover:bg-[var(--color-bg-secondary)] transition-all duration-200">
          Watch Demo →
        </a>
      </div>
</div>
</div>

<!-- Live Stats Counter -->
<section class="stats-counter" id="stats-section">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <div class="stats-grid">
      <div class="stat-item">
        <span class="stat-number" data-target="125">0</span>
        <span class="stat-suffix">+</span>
        <span class="stat-label">AI Commands</span>
      </div>
      <div class="stat-item">
        <span class="stat-number" data-target="500">0</span>
        <span class="stat-suffix">+</span>
        <span class="stat-label">Agencies Worldwide</span>
      </div>
      <div class="stat-item">
        <span class="stat-number" data-target="156000">0</span>
        <span class="stat-suffix"></span>
        <span class="stat-label">Hours Saved/Year</span>
      </div>
      <div class="stat-item">
        <span class="stat-number" data-target="4.9">0</span>
        <span class="stat-suffix">/5</span>
        <span class="stat-label">User Rating</span>
      </div>
    </div>
  </div>
</section>

<!-- Problem Section -->
<section class="py-12 bg-[var(--color-bg-tertiary)]/30 border-y border-[var(--color-border)]">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <h2 id="the-problem" class="gradient-animate text-3xl font-bold text-center mb-8">
      Running an agency is exhausting.
    </h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-4xl mx-auto">
      <div class="flex items-start gap-3 p-4 rounded-lg bg-red-500/10 border border-red-500/20">
        <span class="text-red-500 text-xl">❌</span>
        <span class="text-[var(--color-text-secondary)]">80% of time goes to admin, not clients</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-red-500/10 border border-red-500/20">
        <span class="text-red-500 text-xl">❌</span>
        <span class="text-[var(--color-text-secondary)]">Hiring is expensive and unpredictable</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-red-500/10 border border-red-500/20">
        <span class="text-red-500 text-xl">❌</span>
        <span class="text-[var(--color-text-secondary)]">Scaling means more chaos, not profit</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-red-500/10 border border-red-500/20">
        <span class="text-red-500 text-xl">❌</span>
        <span class="text-[var(--color-text-secondary)]">Always behind on content creation</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-red-500/10 border border-red-500/20">
        <span class="text-red-500 text-xl">❌</span>
        <span class="text-[var(--color-text-secondary)]">Proposals take hours, not minutes</span>
      </div>
    </div>
  </div>
</section>

<!-- Solution Section -->
<section class="py-12">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <h2 id="the-solution" class="gradient-animate text-3xl font-bold text-center mb-8">
      What if AI could run your agency?
    </h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-4xl mx-auto">
      <div class="flex items-start gap-3 p-4 rounded-lg bg-green-500/10 border border-green-500/20">
        <span class="text-green-500 text-xl">✅</span>
        <span class="text-[var(--color-text-secondary)]">125+ AI commands handle everything</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-green-500/10 border border-green-500/20">
        <span class="text-green-500 text-xl">✅</span>
        <span class="text-[var(--color-text-secondary)]">Marketing content in seconds</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-green-500/10 border border-green-500/20">
        <span class="text-green-500 text-xl">✅</span>
        <span class="text-[var(--color-text-secondary)]">Sales proposals that close</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-green-500/10 border border-green-500/20">
        <span class="text-green-500 text-xl">✅</span>
        <span class="text-[var(--color-text-secondary)]">Built-in affiliate income</span>
      </div>
      <div class="flex items-start gap-3 p-4 rounded-lg bg-green-500/10 border border-green-500/20">
        <span class="text-green-500 text-xl">✅</span>
        <span class="text-[var(--color-text-secondary)]">Scale without hiring</span>
      </div>
    </div>
  </div>
</section>

<!-- Stats Counter -->
<section class="py-16 border-b border-[var(--color-border)]">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 max-w-4xl mx-auto text-center">
      
      <div class="stat-card">
        <div class="stat-number text-[var(--color-accent-green)]">125+</div>
        <div class="stat-label">AI Commands</div>
      </div>
      
      <div class="stat-card">
        <div class="stat-number text-[var(--color-accent-blue)]">500+</div>
        <div class="stat-label">Agencies</div>
      </div>
      
      <div class="stat-card">
        <div class="stat-number text-[var(--color-accent-purple)]">4.9/5</div>
        <div class="stat-label">Rating</div>
      </div>
      
      <div class="stat-card">
        <div class="stat-number text-[var(--color-accent-cyan)]">30d</div>
        <div class="stat-label">Guarantee</div>
      </div>
      
    </div>
  </div>
</section>

<style>
  .stat-card {
    padding: 1.5rem;
    border-radius: 1rem;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    transition: all 0.3s ease;
  }
  
  .stat-card:hover {
    transform: translateY(-4px);
    border-color: var(--color-accent-green);
  }
  
  .stat-number {
    font-size: 2.5rem;
    font-weight: 800;
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  
  .stat-label {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    font-weight: 500;
  }
</style>

<!-- Features Section -->
<section class="py-12 bg-[var(--color-bg-tertiary)]/30 border-y border-[var(--color-border)]">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <h2 id="features" class="text-3xl font-bold text-center mb-12 text-[var(--color-text-primary)]">
      Everything Your Agency Needs
    </h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <!-- Marketing -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] hover:border-[var(--color-accent-green)] transition-all">
        <div class="text-3xl mb-4">🔥</div>
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Marketing</h3>
        <p class="text-sm text-[var(--color-text-secondary)] mb-3">/marketing/social</p>
        <p class="text-[var(--color-text-muted)]">Create 30 posts in 30 minutes. Facebook, TikTok, X, LinkedIn.</p>
      </div>

      <!-- Sales -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] hover:border-[var(--color-accent-blue)] transition-all">
        <div class="text-3xl mb-4">💼</div>
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Sales</h3>
        <p class="text-sm text-[var(--color-text-secondary)] mb-3">/sales/proposal</p>
        <p class="text-[var(--color-text-muted)]">AI-powered proposals that win clients. 3x higher close rates.</p>
      </div>

      <!-- Affiliate -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] hover:border-[var(--color-accent-purple)] transition-all">
        <div class="text-3xl mb-4">🎯</div>
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Affiliate</h3>
        <p class="text-sm text-[var(--color-text-secondary)] mb-3">/affiliate/scout</p>
        <p class="text-[var(--color-text-muted)]">50+ PayPal-friendly programs. Passive income on autopilot.</p>
      </div>

      <!-- Finance -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] hover:border-[var(--color-accent-cyan)] transition-all">
        <div class="text-3xl mb-4">💰</div>
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Finance</h3>
        <p class="text-sm text-[var(--color-text-secondary)] mb-3">/finance</p>
        <p class="text-[var(--color-text-muted)]">Track everything automatically. Revenue, expenses, runway.</p>
      </div>

      <!-- Strategy -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] hover:border-[var(--color-accent-yellow)] transition-all">
        <div class="text-3xl mb-4">🏯</div>
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Strategy</h3>
        <p class="text-sm text-[var(--color-text-secondary)] mb-3">/binh-phap</p>
        <p class="text-[var(--color-text-muted)]">Sun Tzu's Art of War for business. 13 strategic clusters.</p>
      </div>

      <!-- Video -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] hover:border-[var(--color-accent-pink)] transition-all">
        <div class="text-3xl mb-4">🎬</div>
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Video</h3>
        <p class="text-sm text-[var(--color-text-secondary)] mb-3">/video/script</p>
        <p class="text-[var(--color-text-muted)]">Generate scripts for YouTube, TikTok, Reels in seconds.</p>
      </div>
    </div>
  </div>
</section>

<!-- How It Works -->
<section class="py-12">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <h2 id="how-it-works" class="text-3xl font-bold text-center mb-12 text-[var(--color-text-primary)]">
      How It Works
    </h2>
    
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8 max-w-4xl mx-auto">
      <div class="text-center">
        <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-[var(--color-accent-green)] flex items-center justify-center text-2xl font-bold text-white">1</div>
        <h3 class="font-semibold mb-2 text-[var(--color-text-primary)]">Get Access</h3>
        <p class="text-sm text-[var(--color-text-muted)]">Sign up via Polar.sh. Instant delivery.</p>
      </div>

      <div class="text-center">
        <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-[var(--color-accent-blue)] flex items-center justify-center text-2xl font-bold text-white">2</div>
        <h3 class="font-semibold mb-2 text-[var(--color-text-primary)]">Open in IDE</h3>
        <p class="text-sm text-[var(--color-text-muted)]">Works with any IDE. Just clone and open.</p>
      </div>

      <div class="text-center">
        <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-[var(--color-accent-purple)] flex items-center justify-center text-2xl font-bold text-white">3</div>
        <h3 class="font-semibold mb-2 text-[var(--color-text-primary)]">Run Commands</h3>
        <p class="text-sm text-[var(--color-text-muted)]">Type /help to see 125+ commands. Pick one.</p>
      </div>

      <div class="text-center">
        <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-[var(--color-accent-cyan)] flex items-center justify-center text-2xl font-bold text-white">4</div>
        <h3 class="font-semibold mb-2 text-[var(--color-text-primary)]">Profit</h3>
        <p class="text-sm text-[var(--color-text-muted)]">Content created. Deals closed. Money made.</p>
      </div>
    </div>
  </div>
</section>

<!-- Pricing Section -->
<section class="py-12 bg-[var(--color-bg-tertiary)]/30 border-y border-[var(--color-border)]">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <h2 id="pricing" class="text-3xl font-bold text-center mb-4 text-[var(--color-text-primary)]">
      Simple Pricing. Massive Value.
    </h2>
    <p class="text-center text-[var(--color-text-muted)] mb-12">All plans include 125+ AI commands</p>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
      <!-- Starter -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)]">
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Starter</h3>
        <div class="text-3xl font-bold mb-4 text-[var(--color-text-primary)]">$29<span class="text-sm font-normal text-[var(--color-text-muted)]">/month</span></div>
        <p class="text-sm text-[var(--color-text-muted)] mb-6">Perfect for solo operators</p>
        <ul class="space-y-2 text-sm text-[var(--color-text-secondary)]">
          <li>✅ 125+ AI commands</li>
          <li>✅ Marketing automation</li>
          <li>✅ Sales proposals</li>
          <li>✅ Community support</li>
        </ul>
      </div>

      <!-- Pro -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border-2 border-[var(--color-accent-green)] relative">
        <div class="absolute -top-3 left-1/2 -translate-x-1/2 px-3 py-1 bg-[var(--color-accent-green)] text-white text-xs font-semibold rounded-full">POPULAR</div>
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Pro</h3>
        <div class="text-3xl font-bold mb-4 text-[var(--color-text-primary)]">$99<span class="text-sm font-normal text-[var(--color-text-muted)]">/month</span></div>
        <p class="text-sm text-[var(--color-text-muted)] mb-6">For serious agency builders</p>
        <ul class="space-y-2 text-sm text-[var(--color-text-secondary)]">
          <li>✅ Everything in Starter</li>
          <li>✅ 5 team seats</li>
          <li>✅ Priority support</li>
          <li>✅ API access</li>
        </ul>
      </div>

      <!-- Franchise -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)]">
        <h3 class="text-lg font-semibold mb-2 text-[var(--color-text-primary)]">Franchise</h3>
        <div class="text-3xl font-bold mb-4 text-[var(--color-text-primary)]">$299<span class="text-sm font-normal text-[var(--color-text-muted)]">/month</span></div>
        <p class="text-sm text-[var(--color-text-muted)] mb-6">White-label & resell</p>
        <ul class="space-y-2 text-sm text-[var(--color-text-secondary)]">
          <li>✅ Everything in Pro</li>
          <li>✅ White-label rights</li>
          <li>✅ Custom domain</li>
          <li>✅ Revenue share (85%)</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<!-- Testimonials -->
<section class="py-16">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-center mb-4 text-[var(--color-text-primary)]">
      What Agencies Say
    </h2>
    <p class="text-center text-[var(--color-text-muted)] mb-12">Real results from real solo agencies</p>
    
    <!-- Video Testimonial -->
    <div class="max-w-2xl mx-auto mb-12">
      <div style="position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; border-radius: 1rem; border: 2px solid var(--color-accent-green); background: var(--color-bg-tertiary);">
        <div style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); text-align: center; padding: 2rem;">
          <div style="font-size: 4rem; margin-bottom: 1rem;">🎬</div>
          <h3 style="margin: 0 0 0.5rem 0; font-size: 1.25rem; color: var(--color-text-primary);">Video Testimonials Coming Soon</h3>
          <p style="margin: 0; color: var(--color-text-muted); font-size: 0.875rem;">Real agency owners sharing their AgencyOS journey</p>
        </div>
      </div>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
      <!-- Testimonial 1 -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] relative hover:border-[var(--color-accent-green)] transition-all">
        <div class="text-2xl mb-3">⭐⭐⭐⭐⭐</div>
        <p class="text-[var(--color-text-secondary)] mb-4 italic text-sm">
          "I replaced my entire team with AgencyOS. Not joking. My profit tripled in 3 months."
        </p>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-gradient-to-r from-[var(--color-accent-green)] to-[var(--color-accent-cyan)] flex items-center justify-center text-white font-bold text-sm">S</div>
          <div>
            <div class="font-semibold text-[var(--color-text-primary)] text-sm">Sarah K.</div>
            <div class="text-xs text-[var(--color-text-muted)]">Marketing Agency</div>
          </div>
        </div>
      </div>

      <!-- Testimonial 2 -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] relative hover:border-[var(--color-accent-green)] transition-all">
        <div class="text-2xl mb-3">⭐⭐⭐⭐⭐</div>
        <p class="text-[var(--color-text-secondary)] mb-4 italic text-sm">
          "From $3K to $15K/month in 90 days. AgencyOS is the unfair advantage I needed."
        </p>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-gradient-to-r from-[var(--color-accent-blue)] to-[var(--color-accent-purple)] flex items-center justify-center text-white font-bold text-sm">M</div>
          <div>
            <div class="font-semibold text-[var(--color-text-primary)] text-sm">Mike T.</div>
            <div class="text-xs text-[var(--color-text-muted)]">Freelancer → Agency</div>
          </div>
        </div>
      </div>

      <!-- Testimonial 3 -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] relative hover:border-[var(--color-accent-green)] transition-all">
        <div class="text-2xl mb-3">⭐⭐⭐⭐⭐</div>
        <p class="text-[var(--color-text-secondary)] mb-4 italic text-sm">
          "The affiliate feature alone pays for itself. 10x ROI in month one."
        </p>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-gradient-to-r from-[var(--color-accent-purple)] to-[var(--color-accent-pink)] flex items-center justify-center text-white font-bold text-sm">D</div>
          <div>
            <div class="font-semibold text-[var(--color-text-primary)] text-sm">David L.</div>
            <div class="text-xs text-[var(--color-text-muted)]">Content Creator</div>
          </div>
        </div>
      </div>

      <!-- Testimonial 4 - NEW -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] relative hover:border-[var(--color-accent-green)] transition-all">
        <div class="text-2xl mb-3">⭐⭐⭐⭐⭐</div>
        <p class="text-[var(--color-text-secondary)] mb-4 italic text-sm">
          "Saved 20+ hours per week. Now I focus on clients, not admin work."
        </p>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-gradient-to-r from-[var(--color-accent-orange)] to-[var(--color-accent-red)] flex items-center justify-center text-white font-bold text-sm">L</div>
          <div>
            <div class="font-semibold text-[var(--color-text-primary)] text-sm">Lisa N.</div>
            <div class="text-xs text-[var(--color-text-muted)]">Design Agency</div>
          </div>
        </div>
      </div>

      <!-- Testimonial 5 - NEW -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] relative hover:border-[var(--color-accent-green)] transition-all">
        <div class="text-2xl mb-3">⭐⭐⭐⭐⭐</div>
        <p class="text-[var(--color-text-secondary)] mb-4 italic text-sm">
          "Binh Pháp strategy changed how I think about competition. Pure genius."
        </p>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-gradient-to-r from-[var(--color-accent-cyan)] to-[var(--color-accent-blue)] flex items-center justify-center text-white font-bold text-sm">T</div>
          <div>
            <div class="font-semibold text-[var(--color-text-primary)] text-sm">Trung V.</div>
            <div class="text-xs text-[var(--color-text-muted)]">Startup Consultant</div>
          </div>
        </div>
      </div>

      <!-- Testimonial 6 - NEW -->
      <div class="p-6 rounded-xl bg-[var(--color-bg-secondary)] border border-[var(--color-border)] relative hover:border-[var(--color-accent-green)] transition-all">
        <div class="text-2xl mb-3">⭐⭐⭐⭐⭐</div>
        <p class="text-[var(--color-text-secondary)] mb-4 italic text-sm">
          "Best $99 I ever spent. Franchise model is printing money for me."
        </p>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-gradient-to-r from-[var(--color-accent-pink)] to-[var(--color-accent-purple)] flex items-center justify-center text-white font-bold text-sm">A</div>
          <div>
            <div class="font-semibold text-[var(--color-text-primary)] text-sm">Alex P.</div>
            <div class="text-xs text-[var(--color-text-muted)]">Agency Network</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Testimonials Carousel -->
<section class="testimonials-section py-16 overflow-hidden">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-center mb-4 text-[var(--color-text-primary)]">
      💬 Loved by Agency Owners
    </h2>
    <p class="text-center text-[var(--color-text-muted)] mb-12">
      See what 500+ agencies say about AgencyOS
    </p>
  </div>
  
  <div class="testimonials-track-wrapper">
    <div class="testimonials-track">
      <!-- Testimonial 1 -->
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"AgencyOS replaced 3 employees for me. ROI was insane within the first week."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #22c55e, #06b6d4);">M</div>
          <div class="info">
            <span class="name">Michael Chen</span>
            <span class="role">Growth Matrix Agency</span>
          </div>
        </div>
      </div>
      
      <!-- Testimonial 2 -->
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"125 commands means I never run out of ways to automate. Best investment this year."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #8b5cf6, #ec4899);">S</div>
          <div class="info">
            <span class="name">Sarah Williams</span>
            <span class="role">Digital First Co</span>
          </div>
        </div>
      </div>
      
      <!-- Testimonial 3 -->
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"/proposal command alone saved me 10+ hours per week on client work."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #f59e0b, #ef4444);">J</div>
          <div class="info">
            <span class="name">James Rodriguez</span>
            <span class="role">JR Creative Studio</span>
          </div>
        </div>
      </div>
      
      <!-- Testimonial 4 -->
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"The Binh Pháp strategy framework is pure gold. Win-without-fighting approach works."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #06b6d4, #3b82f6);">L</div>
          <div class="info">
            <span class="name">Lisa Nguyen</span>
            <span class="role">Scale Partners</span>
          </div>
        </div>
      </div>
      
      <!-- Testimonial 5 -->
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"From solo founder to 6-figure agency in 6 months. AgencyOS is my secret weapon."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #10b981, #34d399);">D</div>
          <div class="info">
            <span class="name">David Park</span>
            <span class="role">Momentum Labs</span>
          </div>
        </div>
      </div>
      
      <!-- Testimonial 6 -->
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"The franchise model is brilliant. Now earning passive income from my agency network."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #f472b6, #a855f7);">A</div>
          <div class="info">
            <span class="name">Anna Thompson</span>
            <span class="role">Agency Empire</span>
          </div>
        </div>
      </div>
      
      <!-- Duplicate for infinite scroll -->
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"AgencyOS replaced 3 employees for me. ROI was insane within the first week."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #22c55e, #06b6d4);">M</div>
          <div class="info">
            <span class="name">Michael Chen</span>
            <span class="role">Growth Matrix Agency</span>
          </div>
        </div>
      </div>
      
      <div class="testimonial-card">
        <div class="stars">⭐⭐⭐⭐⭐</div>
        <p class="quote">"125 commands means I never run out of ways to automate. Best investment this year."</p>
        <div class="author">
          <div class="avatar" style="background: linear-gradient(135deg, #8b5cf6, #ec4899);">S</div>
          <div class="info">
            <span class="name">Sarah Williams</span>
            <span class="role">Digital First Co</span>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <div class="testimonials-fade-left"></div>
  <div class="testimonials-fade-right"></div>
</section>

<!-- Newsletter Signup -->
<section class="py-12 bg-gradient-to-r from-[var(--color-accent-green)]/5 to-[var(--color-accent-cyan)]/5 border-y border-[var(--color-border)]">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-2xl font-bold mb-2 text-[var(--color-text-primary)]">
      📧 Get Weekly Agency Growth Tips
    </h2>
    <p class="text-[var(--color-text-muted)] mb-6">
      Join 1,000+ solo agency owners. No spam, unsubscribe anytime.
    </p>
    
    <form class="flex flex-col sm:flex-row gap-3 max-w-md mx-auto" action="https://agencyos.network/api/subscribe" method="POST">
      <input 
        type="email" 
        name="email" 
        placeholder="your@email.com"
        required
        class="flex-1 px-4 py-3 rounded-lg bg-[var(--color-bg-secondary)] border border-[var(--color-border)] text-[var(--color-text-primary)] placeholder:text-[var(--color-text-muted)] focus:outline-none focus:border-[var(--color-accent-green)]"
      />
      <button 
        type="submit"
        class="px-6 py-3 bg-[var(--color-accent-green)] text-white font-semibold rounded-lg hover:brightness-110 transition-all"
      >
        Subscribe
      </button>
    </form>
    
    <p class="text-xs text-[var(--color-text-muted)] mt-4">
      🔒 We respect your privacy. Read our <a href="/privacy" class="text-[var(--color-accent-green)] hover:underline">Privacy Policy</a>.
    </p>
  </div>
</section>

<!-- Final CTA -->
<section class="py-16">
  <div class="mx-auto max-w-content px-4 sm:px-6 lg:px-8 text-center">
    <h2 id="get-started" class="text-3xl font-bold mb-4 text-[var(--color-text-primary)]">
      Ready to automate your agency?
    </h2>
    <p class="text-lg text-[var(--color-text-muted)] mb-8">
      Start free. Cancel anytime. Win forever.
    </p>
    
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a href="/docs/getting-started/installation" class="inline-flex items-center justify-center px-8 py-3.5 text-base font-medium text-white bg-[var(--color-accent-green)] rounded-[var(--radius-md)] hover:brightness-110 transition-all duration-200 shadow-green hover:shadow-lg transform hover:-translate-y-0.5">
        Start Free Trial
      </a>
      <a href="mailto:hello@agencyos.network" class="inline-flex items-center justify-center px-8 py-3.5 text-base font-medium text-[var(--color-accent-green)] bg-transparent border border-[var(--color-accent-green)] rounded-[var(--radius-md)] hover:bg-[var(--color-accent-green)]/10 transition-all duration-200">
        Book a Demo
      </a>
    </div>
  </div>
</section>

<!-- Floating Promo Badge -->
<div class="promo-badge" id="promo-badge">
  <div class="promo-badge-content">
    <span class="promo-pulse"></span>
    <span class="promo-text">🎄 LAUNCH20</span>
    <span class="promo-discount">-20% OFF</span>
  </div>
  <button class="promo-close" id="promo-close">×</button>
</div>

<!-- Social Proof Popup -->
<div class="social-proof" id="social-proof">
  <div class="social-proof-content">
    <div class="social-proof-avatar" id="proof-avatar">M</div>
    <div class="social-proof-info">
      <span class="social-proof-name" id="proof-name">Michael from US</span>
      <span class="social-proof-action" id="proof-action">just joined AgencyOS Pro</span>
    </div>
    <span class="social-proof-time">2 min ago</span>
  </div>
</div>

</section>

</LandingLayout>

<style>
  /* Floating Promo Badge */
  .promo-badge {
    position: fixed;
    bottom: 24px;
    left: 24px;
    z-index: 1000;
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 16px;
    background: linear-gradient(135deg, rgba(34, 197, 94, 0.9), rgba(245, 158, 11, 0.9));
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(34, 197, 94, 0.3);
    animation: badge-float 3s ease-in-out infinite, badge-glow 2s ease-in-out infinite alternate;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .promo-badge:hover {
    transform: translateY(-4px) scale(1.05);
    box-shadow: 0 12px 40px rgba(34, 197, 94, 0.4);
  }

  .promo-badge-content {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .promo-pulse {
    width: 10px;
    height: 10px;
    background: #fff;
    border-radius: 50%;
    animation: pulse-ring 1.5s infinite;
  }

  .promo-text {
    font-size: 0.9rem;
    font-weight: 700;
    color: white;
  }

  .promo-discount {
    background: rgba(255, 255, 255, 0.2);
    padding: 4px 10px;
    border-radius: 100px;
    font-size: 0.75rem;
    font-weight: 600;
    color: white;
  }

  .promo-close {
    background: rgba(255, 255, 255, 0.2);
    border: none;
    color: white;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    cursor: pointer;
    font-size: 1rem;
    line-height: 1;
    transition: all 0.2s ease;
  }

  .promo-close:hover {
    background: rgba(255, 255, 255, 0.4);
  }

  @keyframes badge-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }

  @keyframes badge-glow {
    0% { box-shadow: 0 8px 32px rgba(34, 197, 94, 0.3); }
    100% { box-shadow: 0 8px 32px rgba(245, 158, 11, 0.4); }
  }

  @keyframes pulse-ring {
    0% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.2); opacity: 0.7; }
    100% { transform: scale(1); opacity: 1; }
  }

  .promo-badge.hidden {
    display: none;
  }

  /* Social Proof Popup */
  .social-proof {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 1000;
    padding: 16px 20px;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    transform: translateX(120%);
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .social-proof.visible {
    transform: translateX(0);
  }

  .social-proof-content {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .social-proof-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, #22c55e, #06b6d4);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 1rem;
  }

  .social-proof-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .social-proof-name {
    font-weight: 600;
    color: var(--color-text-primary);
    font-size: 0.9rem;
  }

  .social-proof-action {
    color: var(--color-text-muted);
    font-size: 0.8rem;
  }

  .social-proof-time {
    font-size: 0.7rem;
    color: var(--color-text-muted);
    margin-left: auto;
  }

  @media (max-width: 768px) {
    .promo-badge {
      left: 12px;
      bottom: 12px;
      padding: 10px 12px;
    }
    
    .social-proof {
      right: 12px;
      bottom: 80px;
      max-width: calc(100vw - 24px);
    }
  }
</style>

<script>
  // Promo Badge
  const promoBadge = document.getElementById('promo-badge');
  const promoClose = document.getElementById('promo-close');
  
  promoClose?.addEventListener('click', (e) => {
    e.stopPropagation();
    promoBadge?.classList.add('hidden');
    localStorage.setItem('promo_closed', 'true');
  });
  
  promoBadge?.addEventListener('click', () => {
    window.location.href = '/pricing?promo=LAUNCH20';
  });
  
  // Hide if already closed
  if (localStorage.getItem('promo_closed') === 'true') {
    promoBadge?.classList.add('hidden');
  }

  // Social Proof Popup
  const socialProof = document.getElementById('social-proof');
  const proofAvatar = document.getElementById('proof-avatar');
  const proofName = document.getElementById('proof-name');
  const proofAction = document.getElementById('proof-action');

  const proofs = [
    { name: 'Michael', country: 'US', action: 'joined Pro', initial: 'M', color: 'linear-gradient(135deg, #22c55e, #06b6d4)' },
    { name: 'Sarah', country: 'UK', action: 'upgraded to Franchise', initial: 'S', color: 'linear-gradient(135deg, #8b5cf6, #ec4899)' },
    { name: 'Lee', country: 'SG', action: 'just started free trial', initial: 'L', color: 'linear-gradient(135deg, #f59e0b, #ef4444)' },
    { name: 'Alex', country: 'DE', action: 'activated license key', initial: 'A', color: 'linear-gradient(135deg, #06b6d4, #3b82f6)' },
    { name: 'Trung', country: 'VN', action: 'joined Starter plan', initial: 'T', color: 'linear-gradient(135deg, #10b981, #34d399)' },
    { name: 'Maria', country: 'BR', action: 'earned 50 AGC referral', initial: 'M', color: 'linear-gradient(135deg, #f472b6, #a855f7)' },
  ];

  let proofIndex = 0;
  
  function showProof() {
    const proof = proofs[proofIndex];
    if (proofAvatar) {
      proofAvatar.textContent = proof.initial;
      proofAvatar.style.background = proof.color;
    }
    if (proofName) proofName.textContent = `${proof.name} from ${proof.country}`;
    if (proofAction) proofAction.textContent = proof.action;
    
    socialProof?.classList.add('visible');
    
    setTimeout(() => {
      socialProof?.classList.remove('visible');
      proofIndex = (proofIndex + 1) % proofs.length;
    }, 4000);
  }

  // Show first proof after 5 seconds, then every 15 seconds
  setTimeout(() => {
    showProof();
    setInterval(showProof, 15000);
  }, 5000);
</script>

<style>
  /* Hero Background Effects */
  .hero-bg-effects {
    position: absolute;
    inset: 0;
    overflow: hidden;
    z-index: 0;
    pointer-events: none;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.4;
  }

  .orb-1 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, #22c55e 0%, transparent 70%);
    top: -100px;
    right: -100px;
    animation: float-1 8s ease-in-out infinite;
  }

  .orb-2 {
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, #06b6d4 0%, transparent 70%);
    bottom: -50px;
    left: -50px;
    animation: float-2 10s ease-in-out infinite;
  }

  .orb-3 {
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, #a855f7 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation: float-3 12s ease-in-out infinite;
  }

  @keyframes float-1 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-30px, 30px) scale(1.1); }
    66% { transform: translate(20px, -20px) scale(0.9); }
  }

  @keyframes float-2 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(40px, -40px) scale(1.15); }
  }

  @keyframes float-3 {
    0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.3; }
    50% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.5; }
  }

  .grid-pattern {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(34, 197, 94, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(34, 197, 94, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 50% at 50% 50%, black 40%, transparent 100%);
    -webkit-mask-image: radial-gradient(ellipse 80% 50% at 50% 50%, black 40%, transparent 100%);
  }

  /* Typewriter Animation Styles */
  .static-text {
    color: var(--color-text-primary);
  }

  .typewriter-container {
    display: inline;
  }

  .typewriter-text {
    background: linear-gradient(135deg, #22c55e, #06b6d4);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .cursor {
    display: inline-block;
    color: #22c55e;
    animation: blink 1s infinite;
    font-weight: 400;
  }

  @keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }

  /* Stats Counter Styles */
  .stats-counter {
    padding: 48px 0;
    background: linear-gradient(180deg, transparent, rgba(34, 197, 94, 0.05));
    border-bottom: 1px solid var(--color-border);
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    max-width: 900px;
    margin: 0 auto;
  }

  .stat-item {
    text-align: center;
    padding: 24px 16px;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    transition: all 0.3s ease;
  }

  .stat-item:hover {
    border-color: #22c55e;
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(34, 197, 94, 0.15);
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 800;
    color: #22c55e;
    display: inline;
  }

  .stat-suffix {
    font-size: 1.5rem;
    font-weight: 600;
    color: #22c55e;
  }

  .stat-label {
    display: block;
    font-size: 0.85rem;
    color: var(--color-text-muted);
    margin-top: 8px;
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .stat-number {
      font-size: 1.75rem;
    }
  }

  /* Testimonials Carousel Styles */
  .testimonials-section {
    position: relative;
  }

  .testimonials-track-wrapper {
    overflow: hidden;
    position: relative;
  }

  .testimonials-track {
    display: flex;
    gap: 24px;
    animation: scroll 30s linear infinite;
    width: max-content;
  }

  .testimonials-track:hover {
    animation-play-state: paused;
  }

  @keyframes scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .testimonial-card {
    flex-shrink: 0;
    width: 320px;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 24px;
    transition: all 0.3s ease;
  }

  .testimonial-card:hover {
    border-color: #22c55e;
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(34, 197, 94, 0.1);
  }

  .testimonial-card .stars {
    font-size: 0.85rem;
    margin-bottom: 12px;
  }

  .testimonial-card .quote {
    color: var(--color-text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
    margin: 0 0 16px 0;
    font-style: italic;
  }

  .testimonial-card .author {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .testimonial-card .avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 1rem;
  }

  .testimonial-card .info {
    display: flex;
    flex-direction: column;
  }

  .testimonial-card .name {
    font-weight: 600;
    color: var(--color-text-primary);
    font-size: 0.9rem;
  }

  .testimonial-card .role {
    color: var(--color-text-muted);
    font-size: 0.8rem;
  }

  .testimonials-fade-left,
  .testimonials-fade-right {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 100px;
    pointer-events: none;
    z-index: 10;
  }

  .testimonials-fade-left {
    left: 0;
    background: linear-gradient(to right, var(--color-bg-primary), transparent);
  }

  .testimonials-fade-right {
    right: 0;
    background: linear-gradient(to left, var(--color-bg-primary), transparent);
  }

  @media (max-width: 768px) {
    .testimonial-card {
      width: 280px;
    }
  }
</style>

<script>
  // Animate numbers when scrolled into view
  function animateValue(element: HTMLElement, start: number, end: number, duration: number) {
    const startTime = performance.now();
    const isFloat = end % 1 !== 0;
    
    function update(currentTime: number) {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);
      
      // easeOutQuart for smooth animation
      const eased = 1 - Math.pow(1 - progress, 4);
      const current = start + (end - start) * eased;
      
      if (isFloat) {
        element.textContent = current.toFixed(1);
      } else if (end >= 1000) {
        element.textContent = Math.floor(current).toLocaleString();
      } else {
        element.textContent = Math.floor(current).toString();
      }
      
      if (progress < 1) {
        requestAnimationFrame(update);
      }
    }
    
    requestAnimationFrame(update);
  }

  // Intersection Observer for triggering animation
  const statsSection = document.getElementById('stats-section');
  let animated = false;

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting && !animated) {
        animated = true;
        const numbers = document.querySelectorAll('.stat-number[data-target]');
        numbers.forEach((el, index) => {
          const target = parseFloat(el.getAttribute('data-target') || '0');
          setTimeout(() => {
            animateValue(el as HTMLElement, 0, target, 2000);
          }, index * 200); // Stagger animation
        });
      }
    });
  }, { threshold: 0.3 });

  if (statsSection) {
    observer.observe(statsSection);
  }

  // Typewriter Animation
  const phrases = [
    'entire agency',
    'marketing',
    'sales',
    'finances',
    'content',
    'operations'
  ];
  
  const typewriterEl = document.getElementById('typewriter');
  let phraseIndex = 0;
  let charIndex = 0;
  let isDeleting = false;
  
  function typeWriter() {
    if (!typewriterEl) return;
    
    const currentPhrase = phrases[phraseIndex];
    
    if (isDeleting) {
      typewriterEl.textContent = currentPhrase.substring(0, charIndex - 1);
      charIndex--;
    } else {
      typewriterEl.textContent = currentPhrase.substring(0, charIndex + 1);
      charIndex++;
    }
    
    let delay = isDeleting ? 50 : 100;
    
    if (!isDeleting && charIndex === currentPhrase.length) {
      delay = 2000; // Pause at end
      isDeleting = true;
    } else if (isDeleting && charIndex === 0) {
      isDeleting = false;
      phraseIndex = (phraseIndex + 1) % phrases.length;
      delay = 500; // Pause before next word
    }
    
    setTimeout(typeWriter, delay);
  }
  
  // Start typewriter after page load
  typeWriter();
</script>

<style>
  /* Magnetic Button Effect */
  .magnetic-btn {
    position: relative;
    transition: transform 0.3s cubic-bezier(0.23, 1, 0.32, 1);
  }

  .magnetic-btn::before {
    content: '';
    position: absolute;
    inset: -20px;
    pointer-events: auto;
  }
</style>

<script>
  // Magnetic Button Effect
  const magneticBtns = document.querySelectorAll('.magnetic-btn');
  
  magneticBtns.forEach(btn => {
    const btnEl = btn as HTMLElement;
    
    btnEl.addEventListener('mousemove', (e: MouseEvent) => {
      const rect = btnEl.getBoundingClientRect();
      const x = e.clientX - rect.left - rect.width / 2;
      const y = e.clientY - rect.top - rect.height / 2;
      
      // Magnetic pull strength (lower = stronger pull)
      const strength = 4;
      
      btnEl.style.transform = `translate(${x / strength}px, ${y / strength}px)`;
    });
    
    btnEl.addEventListener('mouseleave', () => {
      btnEl.style.transform = 'translate(0, 0)';
    });
  });
</script>

<style>
  /* Reveal on Scroll Animation */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .reveal.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  /* Stagger delay classes */
  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }
  .reveal-delay-4 { transition-delay: 0.4s; }
  .reveal-delay-5 { transition-delay: 0.5s; }

  /* Different reveal directions */
  .reveal-left {
    opacity: 0;
    transform: translateX(-30px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .reveal-right {
    opacity: 0;
    transform: translateX(30px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .reveal-left.revealed,
  .reveal-right.revealed {
    opacity: 1;
    transform: translateX(0);
  }

  .reveal-scale {
    opacity: 0;
    transform: scale(0.9);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .reveal-scale.revealed {
    opacity: 1;
    transform: scale(1);
  }
</style>

<script>
  // Reveal on Scroll with Intersection Observer
  const revealElements = document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .reveal-scale');
  
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('revealed');
        // Optionally stop observing after reveal
        // revealObserver.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  });

  revealElements.forEach(el => {
    revealObserver.observe(el);
  });

  // Auto-add reveal class to major sections
  const sections = document.querySelectorAll('section:not(.stats-counter)');
  sections.forEach((section, index) => {
    if (!section.classList.contains('reveal')) {
      section.classList.add('reveal');
      if (index % 2 === 1) {
        section.classList.add('reveal-delay-1');
      }
      revealObserver.observe(section);
    }
  });
</script>

<style>
  /* Animated Gradient Text */
  .gradient-animate {
    background: linear-gradient(
      90deg,
      #22c55e,
      #06b6d4,
      #a855f7,
      #22c55e
    );
    background-size: 300% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradientShift 3s ease infinite;
  }

  @keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  /* Hover pause */
  .gradient-animate:hover {
    animation-play-state: paused;
  }
</style>

